<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        var token = document.querySelector('meta[name="csrf-token"]').getAttribute("content");
    </script>
    <title><%= title %></title>
</head>
<body class="bg-gray-50">
    <%- include('header.ejs') %>
    <div class="flex gap-1 mx-10 mt-6 bg-gray-50">
        
        <a href="/elections" class="text-purple-800 hover:text-purple-600">All Elections</a>
        <span>></span>
        <a href="/elections/<%= id %>/" class="text-purple-800 hover:text-purple-600">Election</a>
    </div>
    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
    <div class="my-6 mx-10">
        <div class="flex gap-2 py-4 justify-between">
            <h5 class="text-2xl"><%= title %> - Questions</h5>
            <div class="bg-black rounded w-fit px-2 py-1 hover:bg-black">
                <a href="/elections/<%= id %>/questions/create" class="text-white">Add New Question</a>
            </div>
        </div>
        <div class="flex-auto">
            <div class="mx-4">  
                <p class="text-gray-600"><%= (questions.length==0)?"You haven't added any questions yet!":"" %></p>
                <div class="grid grid-cols-8 gap-35 border text-blue-400 ">
                    <div class="col-span-3 border px-3">            
                        <p class="py-2 text-2xl">Question</p>                
                    </div>
                    <div class="col-span-3 border">            
                        <p class="py-2 px-10 text-2xl">Description</p>                  
                    </div>
                </div>

                <ol class="list-decimal">
                    <% for (let i=0; i<questions.length; i++) { %>
                        <li>
                            <div class="flex justify-between items-center w-full my-2 px-2 py-1 rounded hover:bg-purple-50">
                                <div>
                                    <a href="/elections/<%= id %>/questions/<%= questions[i].id %>" class="my-2 text-lg text-purple-600 "><%= questions[i].questionName %></a>
                                </div>
                                <div>                                    
                                    <p class="text-purple-800 text-md my-2"><%= questions[i].description %></p>
                                </div>
                            </div>
                        </li>
                    <% } %>
                </ol>
            </div>
        </div>
    </div>
</body>
</html>